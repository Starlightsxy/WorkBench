<script setup lang="uts">
	import { getUserStorage } from '../../storage';

	const userInfo = getUserStorage()
	const activeCategory = ref(0); // 0: 全部, 1: 恋爱, 2: 生日

	const setCategory = (index : number) => {
		activeCategory.value = index;
	};
	onShow(() => {

	})

	onHide(() => {

	})
</script>

<template>
	<view class="container">
		<!-- nav-bar -->
		<view class="navbar">
			<view class="flex-space-between">
				<view class="flex-space-between">
					<image class="nvbar-img" :src="userInfo?.avatar" mode=""></image>
					<view class="pl-5 txt-nvbar">
						<text style="font-size: 20px; font-weight: bold;color: #004fcb;">纪念日</text>
					</view>
				</view>
				<view>
					<image class="nvbar-img" src="/static/image/index/setting.png" mode=""></image>
				</view>
			</view>
		</view>
		<!-- welcome -->
		<scroll-view direction="vertical" style="height: 92%; padding-bottom:20px; ">
			<!-- 1. 置顶纪念日大卡片 (保持不变) -->
			<view class="hero-card-container">
				<image class="hero-image" src="" mode="aspectFill"></image>
				<view class="hero-overlay">
					<view class="hero-tag-row">
						<view class="hero-tag">
							<text class="hero-tag-text">LOVE ANNIVERSARY</text>
						</view>
					</view>
					<text class="hero-title">在一起已经</text>
					<view class="hero-days-row">
						<text class="hero-days-value">520</text>
						<text class="hero-days-unit">天</text>
					</view>
					<view class="hero-date-box">
						<text class="hero-date-icon">📅</text>
						<text class="hero-date-text">起始日：2022-12-25</text>
					</view>
				</view>
			</view>

			<!-- 2. 分类标签栏 (新增点击切换逻辑) -->
			<view class="category-scroll-box">
				<view class="cat-item" :class="{ 'active-cat': activeCategory == 0 }" @click="setCategory(0)">
					<text class="cat-text" :class="{ 'text-white': activeCategory == 0 }">全部</text>
				</view>
				<view class="cat-item" :class="{ 'active-cat': activeCategory == 1 }" @click="setCategory(1)">
					<text class="cat-text" :class="{ 'text-white': activeCategory == 1 }">恋爱</text>
				</view>
				<view class="cat-item" :class="{ 'active-cat': activeCategory == 2 }" @click="setCategory(2)">
					<text class="cat-text" :class="{ 'text-white': activeCategory == 2 }">生日</text>
				</view>
			</view>

			<!-- 3. 纪念日列表容器 -->
			<view class="ann-list-container">
				<text class="section-title">纪念日列表</text>

				<!-- 这里根据 activeCategory 显示不同的列表项 -->
				<!-- 示例：恋爱分类 -->
				<view v-if="activeCategory == 0 || activeCategory == 1" class="ann-item-card">
					<view class="ann-left">
						<view class="ann-icon-bg bg-pink-light">
							<text class="ann-icon-emoji">❤️</text>
						</view>
						<view class="ann-info">
							<text class="ann-name">在一起两周年</text>
							<view class="ann-sub-row">
								<text class="ann-sub-icon">📅</text>
								<text class="ann-sub-text">12月25日</text>
							</view>
						</view>
					</view>
					<view class="ann-right">
						<text class="ann-status-label">还有</text>
						<view class="ann-days-row">
							<text class="ann-days-value color-blue">62</text>
							<text class="ann-days-unit">天</text>
						</view>
					</view>
				</view>

				<!-- 示例：生日分类 -->
				<view v-if="activeCategory == 0 || activeCategory == 2" class="ann-item-card">
					<view class="ann-left">
						<view class="ann-icon-bg bg-blue-light">
							<text class="ann-icon-emoji">🎂</text>
						</view>
						<view class="ann-info">
							<text class="ann-name">妈妈的生日</text>
							<view class="ann-sub-row">
								<text class="ann-sub-icon">📅</text>
								<text class="ann-sub-text">10月15日</text>
							</view>
						</view>
					</view>
					<view class="ann-right">
						<text class="ann-status-label">还有</text>
						<view class="ann-days-row">
							<text class="ann-days-value color-blue">12</text>
							<text class="ann-days-unit">天</text>
						</view>
					</view>
				</view>
			</view>



		</scroll-view>
	</view>
</template>


<style scoped>
	.container {
		background-color: #f9f9ff;
		height: 100%;
		/* 或 100dvh */
		padding-top: var(--status-bar-height);
		/* 状态栏高度 */
		box-sizing: border-box;
		/* 确保 padding 包含在高度内 */

	}

	.navbar {
		justify-content: center;
		height: 8%;
		width: 100%;
		border-bottom: 1px #e0e3e5 solid;
		/* 底部内边距，可调整数值 */
		box-sizing: border-box;

	}

	.nvbar-img {
		width: 44px;
		height: 44px;
		padding: 3px;
		border-radius: 50%;

	}

	.txt-nvbar {
		justify-content: center;
	}

	.category-scroll-box {
		flex-direction: row;
		padding: 0 16px;
		margin-bottom: 24px;
	}

	.cat-item {
		padding: 8px 18px;
		background-color: #f0f3ff;
		border-radius: 12px;
		margin-right: 12px;
		transition: all 0.2s;
	}

	.active-cat {
		background-color: #0066ff;
	}

	.cat-text {
		font-size: 14px;
		color: #1a1a1a;
		font-weight: 500;
	}

	.text-white {
		color: #ffffff;
	}

	.bg-pink-light {
		background-color: #ffebee;
	}

	.bg-blue-light {
		background-color: #e3f2fd;
	}

	.footer-spacer {
		height: 40px;
	}

	/* 置顶大卡片 */
	.hero-card-container {
		position: relative;
		margin: 10px 16px 20px;
		height: 220px;
		border-radius: 24px;
		overflow: hidden;
		background-color: #333;
	}

	.hero-image {
		width: 100%;
		height: 100%;
		opacity: 0.6;
	}

	.hero-overlay {
		position: absolute;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		padding: 24px;
		flex-direction: column;
		justify-content: flex-end;
	}

	.hero-tag-row {
		margin-bottom: 12px;
	}

	.hero-tag {
		background-color: rgba(255, 255, 255, 0.2);
		padding: 4px 10px;
		border-radius: 6px;
		align-self: flex-start;
	}

	.hero-tag-text {
		color: #ffffff;
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.5px;
	}

	.hero-title {
		color: #ffffff;
		font-size: 18px;
		font-weight: 500;
		margin-bottom: 4px;
	}

	.hero-days-row {
		flex-direction: row;
		align-items: baseline;
		margin-bottom: 12px;
	}

	.hero-days-value {
		color: #ffffff;
		font-size: 44px;
		font-weight: 700;
	}

	.hero-days-unit {
		color: #ffffff;
		font-size: 16px;
		margin-left: 6px;
	}

	.hero-date-box {
		flex-direction: row;
		align-items: center;
	}

	.hero-date-icon {
		font-size: 14px;
		margin-right: 6px;
	}

	.hero-date-text {
		color: rgba(255, 255, 255, 0.7);
		font-size: 12px;
	}

	/* 分类标签栏 */
	.category-scroll-box {
		flex-direction: row;
		padding: 0 16px;
		margin-bottom: 24px;
	}

	.cat-item {
		padding: 8px 16px;
		background-color: #f0f3ff;
		border-radius: 12px;
		margin-right: 12px;
	}

	.active-cat {
		background-color: #0066ff;
	}

	.cat-text {
		font-size: 14px;
		color: #1a1a1a;
		font-weight: 500;
	}

	.text-white {
		color: #ffffff;
	}

	/* 纪念日列表 */
	.ann-list-container {
		padding: 0 16px;
	}

	.section-title {
		font-size: 18px;
		font-weight: 700;
		color: #1a1a1a;
		margin-bottom: 16px;
	}

	.ann-item-card {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		background-color: #ffffff;
		padding: 16px;
		border-radius: 20px;
		margin-bottom: 12px;
	}

	.ann-left {
		flex-direction: row;
		align-items: center;
	}

	.ann-icon-bg {
		width: 48px;
		height: 48px;
		border-radius: 14px;
		justify-content: center;
		align-items: center;
		margin-right: 16px;
	}

	.bg-blue-light {
		background-color: #e3f2fd;
	}

	.bg-purple-light {
		background-color: #f3e5f5;
	}

	.bg-gray-light {
		background-color: #f5f5f5;
	}

	.ann-icon-emoji {
		font-size: 20px;
	}

	.ann-info {
		flex-direction: column;
	}

	.ann-name {
		font-size: 16px;
		font-weight: 700;
		color: #1a1a1a;
		margin-bottom: 4px;
	}

	.ann-sub-row {
		flex-direction: row;
		align-items: center;
	}

	.ann-sub-icon {
		font-size: 11px;
		margin-right: 4px;
	}

	.ann-sub-text {
		font-size: 12px;
		color: #7b87a0;
	}

	.ann-right {
		flex-direction: column;
		align-items: flex-end;
	}

	.ann-status-label {
		font-size: 11px;
		color: #7b87a0;
		margin-bottom: 2px;
	}

	.ann-days-row {
		flex-direction: row;
		align-items: baseline;
	}

	.ann-days-value {
		font-size: 22px;
		font-weight: 700;
		color: #1a1a1a;
	}

	.color-blue {
		color: #0066ff;
	}

	.ann-days-unit {
		font-size: 12px;
		color: #1a1a1a;
		margin-left: 2px;
	}
</style>
